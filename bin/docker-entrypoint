#!/bin/bash -e

# Prepare all databases (primary, cache, queue, cable) on every boot.
# db:prepare is idempotent: creates the DB if missing, loads schema if empty,
# runs pending migrations otherwise. Required because consumer/jobs commands
# bypass the rails-server-only branch that previously gated this step.
./bin/rails db:prepare

exec "${@}"
